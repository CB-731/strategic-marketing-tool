# Original Specification Summary

The full user-provided specification is represented across these planning files:

- `AI_PROJECT_CONTEXT.md` for core requirements and hard rules.
- `docs/TASK_CONTENT_MATRIX.md` for task names, file names, template placeholders, diagram placeholders, checklists, and links.
- `docs/SPRINTS.md` for implementation order.
- `docs/ACCEPTANCE_CRITERIA.md` for final behavior checks.
- `docs/ARCHITECTURE.md` for implementation structure.

If any future session needs the exact long-form right sidebar copy, use the original chat prompt as the canonical source. During implementation, every right sidebar section for all 12 tasks must be hardcoded into the app data object.

Important: Do not invent task content. If exact copy is not available in context, ask for it or retrieve it from the original prompt.
